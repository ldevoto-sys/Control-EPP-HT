{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && rm -f package-lock.json && npm install && npm run build && cd ../backend && rm -f package-lock.json && npm install"
  },
  "deploy": {
    "startCommand": "node backend/server.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
